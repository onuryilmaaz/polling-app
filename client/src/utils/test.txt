<div
  class="relative flex flex-col items-center justify-center bg-gradient-to-b from-gray-900 to-black text-white py-16"
>
  <div class="max-w-2xl mx-auto p-6 bg-white shadow-lg rounded-lg">
    <h2 class="text-2xl font-bold mb-6 text-center">Yeni Anket Oluştur</h2>

    <form (ngSubmit)="onSubmit()" #pollForm="ngForm" class="space-y-8">
      <!-- Anket Başlığı -->
      <mat-form-field appearance="fill" class="w-full">
        <mat-label>Anket Başlığı</mat-label>
        <input
          maxlength="50"
          minlength="5"
          [formControl]="titleFormControl"
          matInput
          type="text"
          id="title"
          name="title"
          [(ngModel)]="poll.title"
          required
        />
        <mat-error *ngIf="titleFormControl.hasError('required')">
          Başlık <strong>zorunlu</strong>
        </mat-error>
        <mat-error *ngIf="titleFormControl.hasError('minlength')"
          >En az 5 karakter</mat-error
        >
        <mat-error *ngIf="titleFormControl.hasError('maxlength')"
          >En fazla 50 karakter</mat-error
        >
      </mat-form-field>

      <!-- Açıklama -->
      <mat-form-field appearance="fill" class="w-full">
        <mat-label>Açıklama</mat-label>
        <textarea
          matInput
          id="description"
          name="description"
          [(ngModel)]="poll.description"
        ></textarea>
        <mat-error *ngIf="titleFormControl.hasError('maxlength')"
          >En fazla 150 karakter</mat-error
        >
      </mat-form-field>

      <!-- Başlangıç Tarihi -->
      <mat-form-field appearance="fill" class="w-full">
        <mat-label>Başlangıç Tarihi</mat-label>
        <input
          matInput
          [matDatepicker]="picker1"
          id="createdDate"
          name="createdDate"
          [(ngModel)]="poll.createdDate"
        />
        <mat-datepicker-toggle
          matSuffix
          [for]="picker1"
        ></mat-datepicker-toggle>
        <mat-datepicker #picker1></mat-datepicker>
      </mat-form-field>

      <!-- Bitiş Tarihi -->
      <mat-form-field appearance="fill" class="w-full">
        <mat-label>Bitiş Tarihi</mat-label>
        <input
          matInput
          [matDatepicker]="picker2"
          id="expiryDate"
          name="expiryDate"
          [(ngModel)]="poll.expiryDate"
        />
        <mat-datepicker-toggle
          matSuffix
          [for]="picker2"
        ></mat-datepicker-toggle>
        <mat-datepicker #picker2></mat-datepicker>
      </mat-form-field>

      <!-- Aktif Mi? -->
      <mat-checkbox
        id="isActive"
        name="isActive"
        [(ngModel)]="poll.isActive"
        class="mt-2"
      >
        Aktif Mi?
      </mat-checkbox>

      <!-- Soru Listesi -->
      <div class="space-y-6">
        <div
          *ngFor="let question of poll.questions; let i = index"
          class="relative group"
        >
          <!-- Soru Kartı -->
          <mat-card
            class="p-4 mb-6 bg-indigo-50 border-l-4 border-indigo-400 shadow-sm"
          >
            <div class="absolute top-2 right-2 opacity-70">
              <mat-icon class="text-indigo-300">help_outline</mat-icon>
            </div>

            <div class="flex justify-between items-center mb-4">
              <h3
                class="text-lg font-bold text-indigo-700 flex items-center gap-2"
              >
                <mat-icon class="icon-outlined">quiz</mat-icon>
                Soru {{ i + 1 }}
              </h3>
              <button
                mat-icon-button
                type="button"
                (click)="removeQuestion(i)"
                class="text-red-500 hover:bg-red-50"
              >
                <mat-icon>delete_forever</mat-icon>
              </button>
            </div>

            <!-- Soru Metni -->
            <div class="space-y-4 pl-2">
              <!-- Soru Metni ve Diğer Alanlar -->
              <mat-form-field appearance="outline" class="w-full">
                <mat-label>Soru Metni</mat-label>
                <input matInput [(ngModel)]="question.text" required />
                <mat-icon matPrefix>question_mark</mat-icon>
              </mat-form-field>

              <!-- Soru Türü -->
              <mat-form-field appearance="fill" class="w-full">
                <mat-label>Soru Türü</mat-label>
                <mat-select
                  [id]="'questionType' + i"
                  [name]="'questionType' + i"
                  [(ngModel)]="question.type"
                >
                  <mat-option value="0">Tek Seçim</mat-option>
                  <mat-option value="1">Metin</mat-option>
                  <mat-option value="2">Doğru Yanlış</mat-option>
                  <mat-option value="3">Çoklu Seçim</mat-option>
                  <mat-option value="4">Sıralama</mat-option>
                </mat-select>
              </mat-form-field>

              <!-- Zorunlu Mu? -->
              <mat-checkbox
                [id]="'isRequired' + i"
                [name]="'isRequired' + i"
                [(ngModel)]="question.isRequired"
              >
                Zorunlu Mu?
              </mat-checkbox>

              <!-- Maksimum Seçim Sayısı -->
              <mat-form-field appearance="fill" class="w-full">
                <mat-label>Maksimum Seçim Sayısı</mat-label>
                <input
                  matInput
                  type="number"
                  [id]="'maxSelections' + i"
                  [name]="'maxSelections' + i"
                  [(ngModel)]="question.maxSelections"
                />
              </mat-form-field>

              <!-- Seçenekler -->
              <!-- Seçenekler Listesi -->
              <div class="space-y-3 ml-4">
                <div
                  *ngFor="let option of question.options; let j = index"
                  class="bg-purple-50 p-3 rounded-lg border border-purple-100"
                >
                  <div class="flex items-center justify-between mb-2">
                    <h4
                      class="text-sm font-medium text-purple-700 flex items-center gap-2"
                    >
                      <mat-icon class="text-sm icon-outlined"
                        >list_alt</mat-icon
                      >
                      Seçenek {{ j + 1 }}
                    </h4>
                    <button
                      mat-icon-button
                      (click)="removeOption(i, j)"
                      class="text-red-400 hover:bg-red-50"
                    >
                      <mat-icon class="text-base">clear</mat-icon>
                    </button>
                  </div>
                  <mat-form-field appearance="outline" class="w-full">
                    <mat-label>Seçenek Metni</mat-label>
                    <input matInput [(ngModel)]="option.text" required />
                    <mat-icon matPrefix>radio_button_unchecked</mat-icon>
                  </mat-form-field>
                </div>
              </div>

              <!-- Seçenek Ekle Butonu -->
              <button
                mat-stroked-button
                type="button"
                (click)="addOption(i)"
                class="w-full border-dashed border-2 border-purple-200 text-purple-600 hover:border-purple-400 mt-3"
              >
                <mat-icon>add_circle_outline</mat-icon>
                Seçenek Ekle
              </button>
            </div>
          </mat-card>
        </div>
      </div>

      <!-- Soru Ekle Butonu -->
      <button
        mat-raised-button
        type="button"
        color="primary"
        (click)="addQuestion()"
        class="w-full !bg-indigo-600 !text-white hover:!bg-indigo-700 h-12"
      >
        <mat-icon>add_task</mat-icon>
        Yeni Soru Ekle
      </button>

      <!-- Anketi Oluştur Butonu -->
      <button
        mat-raised-button
        type="submit"
        color="accent"
        [disabled]="!pollForm.form.valid"
        class="w-full"
      >
        Anketi Oluştur
      </button>
    </form>
  </div>
</div>

---------------
<div
  class="relative flex flex-col items-center justify-center bg-gradient-to-b from-gray-900 to-black text-white py-16"
>
  <div class="max-w-2xl mx-auto p-6 bg-white shadow-lg rounded-lg">
    <h2 class="text-2xl font-bold mb-6 text-center">Anketi Düzenle</h2>

    <form (ngSubmit)="onSubmit()" #pollForm="ngForm" class="space-y-6">
      <!-- Anket Başlığı -->
      <mat-form-field appearance="fill" class="w-full">
        <mat-label>Anket Başlığı</mat-label>
        <input
          maxlength="50"
          minlength="5"
          [formControl]="titleFormControl"
          matInput
          type="text"
          id="title"
          name="title"
          [(ngModel)]="poll.title"
          required
        />
        <mat-error *ngIf="titleFormControl.hasError('required')">
          Başlık <strong>zorunlu</strong>
        </mat-error>
        <mat-error *ngIf="titleFormControl.hasError('minlength')"
          >En az 5 karakter</mat-error
        >
        <mat-error *ngIf="titleFormControl.hasError('maxlength')"
          >En fazla 50 karakter</mat-error
        >
      </mat-form-field>

      <!-- Açıklama -->
      <mat-form-field appearance="fill" class="w-full">
        <mat-label>Açıklama</mat-label>
        <textarea
          matInput
          id="description"
          name="description"
          [(ngModel)]="poll.description"
        ></textarea>
      </mat-form-field>

      <!-- Bitiş Tarihi -->
      <mat-form-field appearance="fill" class="w-full">
        <mat-label>Bitiş Tarihi</mat-label>
        <input
          matInput
          [matDatepicker]="picker"
          id="expiryDate"
          name="expiryDate"
          [(ngModel)]="poll.expiryDate"
        />
        <mat-datepicker-toggle matSuffix [for]="picker"></mat-datepicker-toggle>
        <mat-datepicker #picker></mat-datepicker>
      </mat-form-field>

      <!-- Aktif Mi? -->
      <mat-checkbox
        id="isActive"
        name="isActive"
        [(ngModel)]="poll.isActive"
        class="mt-2"
      >
        Aktif Mi?
      </mat-checkbox>

      <!-- Sorular -->
      <div
        *ngFor="let question of poll.questions; let i = index"
        class="space-y-4 p-4 border border-gray-200 rounded-lg"
      >
        <div class="flex justify-between items-center">
          <h3 class="text-lg font-semibold">Soru {{ i + 1 }}</h3>
          <button mat-icon-button type="button" (click)="removeQuestion(i)">
            <mat-icon color="warn">delete</mat-icon>
          </button>
        </div>

        <!-- Soru Metni -->
        <mat-form-field appearance="fill" class="w-full">
          <mat-label>Soru Metni</mat-label>
          <input
            matInput
            [id]="'questionText' + i"
            [name]="'questionText' + i"
            [(ngModel)]="question.text"
            required
          />
        </mat-form-field>

        <!-- Soru Türü -->
        <mat-form-field appearance="fill" class="w-full">
          <mat-label>Soru Türü</mat-label>
          <mat-select
            [id]="'questionType' + i"
            [name]="'questionType' + i"
            [(ngModel)]="question.type"
          >
            <mat-option [value]="0">Tek Seçim</mat-option>
            <mat-option [value]="1">Metin</mat-option>
            <mat-option [value]="2">Doğru Yanlış</mat-option>
            <mat-option [value]="3">Çoklu Seçim</mat-option>
            <mat-option [value]="4">Sıralama</mat-option>
          </mat-select>
        </mat-form-field>

        <!-- Zorunlu Mu? -->
        <mat-checkbox
          [id]="'isRequired' + i"
          [name]="'isRequired' + i"
          [(ngModel)]="question.isRequired"
        >
          Zorunlu Mu?
        </mat-checkbox>

        <!-- Maksimum Seçim Sayısı -->
        <mat-form-field appearance="fill" class="w-full">
          <mat-label>Maksimum Seçim Sayısı</mat-label>
          <input
            matInput
            type="number"
            [id]="'maxSelections' + i"
            [name]="'maxSelections' + i"
            [(ngModel)]="question.maxSelections"
          />
        </mat-form-field>

        <!-- Seçenekler -->
        <div
          *ngFor="let option of question.options; let j = index"
          class="space-y-2 p-3 border border-gray-100 rounded-md"
        >
          <div class="flex justify-between items-center">
            <h4 class="text-md font-medium">Seçenek {{ j + 1 }}</h4>
            <button mat-icon-button type="button" (click)="removeOption(i, j)">
              <mat-icon color="warn">delete</mat-icon>
            </button>
          </div>
          <mat-form-field appearance="fill" class="w-full">
            <mat-label>Seçenek Metni</mat-label>
            <input
              matInput
              [id]="'optionText' + i + '-' + j"
              [name]="'optionText' + i + '-' + j"
              [(ngModel)]="option.text"
              required
            />
          </mat-form-field>
        </div>

        <!-- Seçenek Ekle Butonu -->
        <button
          mat-button
          type="button"
          (click)="addOption(i)"
          class="w-full bg-indigo-100 text-indigo-700 px-4 py-2 rounded-md hover:bg-indigo-200 focus:outline-none focus:ring-2 focus:ring-indigo-500"
        >
          Seçenek Ekle
        </button>
      </div>

      <!-- Soru Ekle Butonu -->
      <button
        mat-raised-button
        type="button"
        color="primary"
        (click)="addQuestion()"
        class="w-full"
      >
        Soru Ekle
      </button>

      <!-- Anketi Güncelle Butonu -->
      <button
        mat-raised-button
        type="submit"
        color="accent"
        [disabled]="!pollForm.form.valid"
        class="w-full"
      >
        Anketi Güncelle
      </button>
    </form>
  </div>
</div>
-----------
<!-- Ranking -->
          <div *ngIf="question.type === questionType.Ranking" class="space-y-4">
            <p class="text-sm text-gray-500">Sıralama yapınız (1: En yüksek)</p>
            <div
              *ngFor="let option of question.options"
              class="flex items-center space-x-2"
            >
              <select
                [(ngModel)]="rankedOptions[question.id][option.id]"
                [name]="'rank_' + question.id + '_' + option.id"
                class="p-1 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 text-black"
              >
                <option [ngValue]="null">Seçiniz</option>
                <option
                  *ngFor="let i of getNumberArray(question.maxSelections)"
                  [ngValue]="i"
                >
                  {{ i }}
                </option>
              </select>
              <label class="text-gray-700">{{ option.text }}</label>
            </div>
          </div>